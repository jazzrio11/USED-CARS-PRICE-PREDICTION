import os
import pickle
import numpy as np
import pandas as pd

#
# other_features = ['Year', 'Kilometers_Driven', 'Mileage', 'Engine',
#                   'Power', 'Seats']
#
# other_f = np.zeros(len(other_features))
#
# other_f[0]=2012
#
#
# cars_f = np.where(other_f.columns == other_features.index("Year"))[0][0]
#
#
# print(cars_f)

print(int("s"))